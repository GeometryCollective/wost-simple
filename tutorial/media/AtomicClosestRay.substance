-- Draw a ray hit and miss
Point a, b
Segment s := LineSegment(a,b)
Point x
Vector v


Label a "$\vec{a}$"
Label b "$\vec{b}$"
Label x "$\vec{x}$"

RootedAt(x,v)

Ray r := RayFrom(x,v)

Point y := RaySegmentIntersection(r,s)

