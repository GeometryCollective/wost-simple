-- Draw a bunch of examples of point-segment closest point queries
Point a, b
Segment ab := LineSegment(a,b)

Point x
Point y := ClosestPoint(ab,x)
Segment s := ClosestSegment(ab,x)

Length l := LengthBetween(a,y)

-- Label a "$\vec{a}$"
-- Label b "$\vec{b}$"
-- Label x1 "$\vec{x}$"
Label a "a"
Label b "b"
Label x "x"
Label l "t"
