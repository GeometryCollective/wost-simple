-- Draw a point-segment closest point query,
-- labeling the distance t between the first
-- endpoint and the intersection point
Point a, b
Let ab := Segment(a,b)

Point x
Point y := ClosestPoint(ab,x)
Segment s := ClosestSegment(ab,x)

Length l := LengthBetween(a,y)

Label a $a$
Label b $b$
Label x $x$
Label y $y$
Label l $t$
